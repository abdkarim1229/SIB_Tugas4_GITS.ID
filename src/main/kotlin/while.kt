fun main() {
    var list = arrayListOf<String>("Rendang", "Pangsit", "Nasi Goreng", "Mie Goreng", "Sate", "Bakso", "Ayam Bakar")
    var index = 0
    while (index < list.size) {
        list.forEach { index -> println(list) }
        index++
    }
}